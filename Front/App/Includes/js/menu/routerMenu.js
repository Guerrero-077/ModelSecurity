import { Menu } from './menu.js';

export const menuItems = [
    { name: 'Person', href: '#/person' },
    { name: 'User', href: '# '},
    { name: 'Form', href: '#/form' },
    { name: 'Module', href: '#' }

];

const menu = new Menu(menuItems);